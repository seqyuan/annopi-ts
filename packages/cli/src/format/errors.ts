export function formatUserError(message: string): string {
  return `annopi: ${message}`
}

export function formatRuntimeError(message: string): string {
  return `annopi runtime error: ${message}`
}
